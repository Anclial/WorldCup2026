{
  "name": "worldcup2026-pickem",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules --project worldcup2026pickem",
    "clear:firestore": "node scripts/clear-firestore.mjs",
    "migrate:firestore": "node scripts/migrate-to-firestore.mjs",
    "sync:results": "node scripts/sync-results.mjs",
    "reset:firestore": "node scripts/clear-firestore.mjs && node scripts/migrate-to-firestore.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}
