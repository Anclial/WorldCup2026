{
  "name": "worldcup2026-pickem",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:rules": "firebase deploy --only firestore:rules --project worldcup2026pickem",
    "migrate:firestore": "node scripts/migrate-to-firestore.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}
