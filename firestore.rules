rules_version = '2';

// Paste into Firebase Console → Firestore → Rules.
// Tighten with Cloud Functions later; open enough for the pick'em app to read/write.
service cloud.firestore {
  match /databases/{database}/documents {
    match /players/{playerId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    match /rosters/{playerId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    match /results/{teamId} {
      allow read: if true;
      allow create, update, delete: if true;
    }

    match /config/{docId} {
      allow read: if true;
      allow write: if docId == 'settings';
    }
  }
}
